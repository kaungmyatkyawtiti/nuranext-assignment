import Logo from './Logo';
import Navbar from './Navbar';

const Header = () => {
  return (
    <header>
      {/* Logo */}
      <Logo />

      {/* Navbar */}
      <Navbar />
    </header>
  );
};

export default Header;
